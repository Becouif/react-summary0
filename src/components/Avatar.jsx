import React from "react";

function Avatar(props){
  return (
    <img src={props.img} alt="someone"/>
  )
}

export default Avatar;


// after creating avatar component then use in card with nested props 
